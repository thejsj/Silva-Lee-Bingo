{
  "name": "bingo-game-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:game": "pnpm --filter game dev",
    "dev:control": "pnpm --filter control-plane dev",
    "deploy": "pnpm run deploy:game && pnpm run deploy:control",
    "deploy:game": "cd apps/game && pnpm build && netlify deploy --prod",
    "deploy:control": "cd apps/control-plane && pnpm build && netlify deploy --prod"
  },
  "devDependencies": {},
  "pnpm": {
    "overrides": {}
  }
}
